// PageSeparator removed — on-screen pagination removed. Keeping a small
// placeholder so imports won't break if referenced by any leftover code.
import React from 'react';
export default function PageSeparator() { return null; }
